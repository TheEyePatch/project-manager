development:
  adapter: redis
  url: <%= ENV.fetch("ACTION_CABLE_URL") %>
  channel_prefix: task_master_dev

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: project_manager_production
